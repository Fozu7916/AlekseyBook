{
  "ConnectionStrings": {
    "DefaultConnection": "#{ConnectionStrings__DefaultConnection}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Jwt": {
    "Key": "#{JWT_SECRET}",
    "Issuer": "#{API_URL}",
    "Audience": "#{CLIENT_URL}"
  }
}
